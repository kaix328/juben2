# 分镜优化实施方案

## 🎯 优化目标

1. **提升提示词质量** - 确保提示词完整、准确、有效
2. **增强连贯性** - 场景内外的逻辑连贯性
3. **提高一致性** - 角色、环境、风格的一致性
4. **优化用户体验** - 自动检测和修复问题

---

## 📋 实施计划

### 阶段一：提示词验证器（优先级：高）

**目标**：确保每个分镜的提示词完整且有效

**实施步骤**：

1. 创建 `src/app/utils/promptValidator.ts`
2. 集成到分镜生成流程
3. 在UI中显示验证结果

**预期效果**：
- 提示词完整率从 70% 提升到 95%
- 减少空提示词和无效提示词

### 阶段二：连贯性增强（优先级：高）

**目标**：检测和修复场景内的连贯性问题

**实施步骤**：

1. 扩展 `qualityChecker.ts`
2. 添加场景级连贯性检查
3. 实现自动修复建议

**预期效果**：
- 连贯性评分从 75 提升到 90
- 自动发现 80% 的连贯性问题

### 阶段三：智能优化（优先级：中）

**目标**：使用AI优化提示词和补充细节

**实施步骤**：

1. 创建提示词压缩器
2. 实现平台适配器
3. 添加AI智能增强（可选）

**预期效果**：
- 提示词质量提升 20%
- 支持多平台优化

---

## 💻 代码实现

### 1. 提示词验证器

创建文件：`src/app/utils/promptValidator.ts`

```typescript
import type { StoryboardPanel, Character, Scene } from '../types';

export interface PromptValidationResult {
    valid: boolean;
    score: number; // 0-100
    issues: ValidationIssue[];
    suggestions: string[];
}

export interface ValidationIssue {
    type: 'missing' | 'incomplete' | 'toolong' | 'inconsistent';
    severity: 'error' | 'warning' | 'info';
    message: string;
    field?: string;
}

/**
 * 验证分镜提示词质量
 */
export function validatePrompt(
    panel: StoryboardPanel,
    characters: Character[],
    scenes: Scene[]
): PromptValidationResult {
    const issues: ValidationIssue[] = [];
    const suggestions: string[] = [];
    let score = 100;

    // 1. 检查绘画提示词
    if (!panel.aiPrompt || panel.aiPrompt.trim() === '') {
        issues.push({
            type: 'missing',
            severity: 'error',
            message: '缺少绘画提示词',
            field: 'aiPrompt'
        });
        suggestions.push('点击"刷新提示词"按钮生成提示词');
        score -= 30;
    } else {
        // 检查长度
        const length = panel.aiPrompt.length;
        if (length < 40) {
            issues.push({
                type: 'incomplete',
                severity: 'warning',
                message: `提示词过短（${length}字）`,
                field: 'aiPrompt'
            });
            suggestions.push('添加更多场景、氛围和细节描述');
            score -= 15;
        } else if (length > 300) {
            issues.push({
                type: 'toolong',
                severity: 'warning',
                message: `提示词过长（${length}字）`,
                field: 'aiPrompt'
            });
            suggestions.push('精简次要信息，保留核心描述');
            score -= 10;
        }

        // 检查角色信息
        if (panel.characters.length > 0) {
            const prompt = panel.aiPrompt.toLowerCase();
            const missingChars = panel.characters.filter(
                name => !prompt.includes(name.toLowerCase())
            );
            if (missingChars.length > 0) {
                issues.push({
                    type: 'incomplete',
                    severity: 'warning',
                    message: `缺少角色: ${missingChars.join('、')}`,
                    field: 'aiPrompt'
                });
                suggestions.push('在提示词中添加角色名称和触发词');
                score -= 10;
            }
        }

        // 检查景别信息
        if (!panel.aiPrompt.includes(panel.shot)) {
            issues.push({
                type: 'incomplete',
                severity: 'info',
                message: '提示词中缺少景别信息',
                field: 'aiPrompt'
            });
            suggestions.push(`添加景别描述: ${panel.shot}`);
            score -= 5;
        }
    }

    // 2. 检查视频提示词
    if (!panel.aiVideoPrompt || panel.aiVideoPrompt.trim() === '') {
        if ((panel.duration || 0) >= 3) {
            issues.push({
                type: 'missing',
                severity: 'warning',
                message: '缺少视频提示词',
                field: 'aiVideoPrompt'
            });
            suggestions.push('为视频生成添加视频提示词');
            score -= 10;
        }
    }

    // 3. 检查关键字段
    const requiredFields = [
        { field: 'description', name: '画面描述' },
        { field: 'shot', name: '景别' },
        { field: 'angle', name: '角度' },
        { field: 'duration', name: '时长' }
    ];

    requiredFields.forEach(({ field, name }) => {
        const value = (panel as any)[field];
        if (!value || (typeof value === 'string' && value.trim() === '')) {
            issues.push({
                type: 'missing',
                severity: 'error',
                message: `缺少${name}`,
                field
            });
            score -= 15;
        }
    });

    // 4. 检查视频必需字段
    if ((panel.duration || 0) >= 3) {
        if (!panel.startFrame || panel.startFrame.trim() === '') {
            issues.push({
                type: 'missing',
                severity: 'warning',
                message: '缺少起始帧描述',
                field: 'startFrame'
            });
            suggestions.push('添加起始帧画面描述');
            score -= 5;
        }
        if (!panel.endFrame || panel.endFrame.trim() === '') {
            issues.push({
                type: 'missing',
                severity: 'warning',
                message: '缺少结束帧描述',
                field: 'endFrame'
            });
            suggestions.push('添加结束帧画面描述');
            score -= 5;
        }
    }

    return {
        valid: issues.filter(i => i.severity === 'error').length === 0,
        score: Math.max(0, score),
        issues,
        suggestions
    };
}

/**
 * 批量验证所有分镜
 */
export function validateAllPanels(
    panels: StoryboardPanel[],
    characters: Character[],
    scenes: Scene[]
): {
    totalScore: number;
    validCount: number;
    invalidCount: number;
    results: Map<string, PromptValidationResult>;
} {
    const results = new Map<string, PromptValidationResult>();
    let totalScore = 0;
    let validCount = 0;

    panels.forEach(panel => {
        const result = validatePrompt(panel, characters, scenes);
        results.set(panel.id, result);
        totalScore += result.score;
        if (result.valid) validCount++;
    });

    return {
        totalScore: panels.length > 0 ? Math.round(totalScore / panels.length) : 0,
        validCount,
        invalidCount: panels.length - validCount,
        results
    };
}
```

### 2. 连贯性检查增强

扩展文件：`src/app/utils/ai/qualityChecker.ts`

在文件末尾添加：

```typescript
/**
 * 场景级连贯性检查
 */
function checkSceneLevelContinuity(panels: any[]): QualityIssue[] {
    const issues: QualityIssue[] = [];
    
    // 按场景分组
    const sceneGroups = new Map<string, any[]>();
    panels.forEach(panel => {
        if (!sceneGroups.has(panel.sceneId)) {
            sceneGroups.set(panel.sceneId, []);
        }
        sceneGroups.get(panel.sceneId)!.push(panel);
    });
    
    sceneGroups.forEach((scenePanels, sceneId) => {
        // 1. 检查建立镜头
        const hasEstablishing = scenePanels.some(p => 
            p.shot === '远景' || p.shot === '全景' || p.shot === '大远景'
        );
        if (!hasEstablishing && scenePanels.length > 2) {
            issues.push({
                id: generateIssueId(IssueType.CONTINUITY, scenePanels[0].panelNumber),
                type: IssueType.CONTINUITY,
                severity: IssueSeverity.WARNING,
                panelNumber: scenePanels[0].panelNumber,
                panelId: scenePanels[0].id,
                message: '场景缺少建立镜头',
                suggestion: '在场景开始添加远景或全景镜头'
            });
        }
        
        // 2. 检查角色连贯性
        const allCharacters = new Set<string>();
        scenePanels.forEach(p => p.characters?.forEach((c: string) => allCharacters.add(c)));
        
        scenePanels.forEach((panel, index) => {
            if (index > 0) {
                const prevChars = new Set(scenePanels[index - 1].characters || []);
                const currChars = new Set(panel.characters || []);
                
                // 检查角色突然消失
                prevChars.forEach(char => {
                    if (!currChars.has(char) && index < scenePanels.length - 1) {
                        issues.push({
                            id: generateIssueId(IssueType.CHARACTER, panel.panelNumber),
                            type: IssueType.CHARACTER,
                            severity: IssueSeverity.INFO,
                            panelNumber: panel.panelNumber,
                            panelId: panel.id,
                            message: `角色"${char}"在场景中途消失`,
                            suggestion: '如果角色离开，建议添加离开镜头'
                        });
                    }
                });
            }
        });
        
        // 3. 检查环境一致性
        const environments = scenePanels
            .map(p => p.environmentMotion)
            .filter(e => e && e.trim() !== '');
        
        if (environments.length > 0) {
            const uniqueEnvs = new Set(environments);
            if (uniqueEnvs.size > 3) {
                issues.push({
                    id: generateIssueId(IssueType.CONTINUITY, scenePanels[0].panelNumber),
                    type: IssueType.CONTINUITY,
                    severity: IssueSeverity.INFO,
                    panelNumber: scenePanels[0].panelNumber,
                    panelId: scenePanels[0].id,
                    message: '场景内环境变化过多',
                    suggestion: '保持场景内环境描述的一致性'
                });
            }
        }
    });
    
    return issues;
}

// 在 performQualityCheck 函数中添加调用
// 在 "if (finalConfig.checkContinuity)" 块中添加：
if (finalConfig.checkContinuity) {
    const issues = checkContinuity(panels);
    allIssues.push(...issues);
    
    // 🆕 添加场景级连贯性检查
    const sceneIssues = checkSceneLevelContinuity(panels);
    allIssues.push(...sceneIssues);
    
    console.log(`[QualityCheck] 连贯性检查: ${issues.length + sceneIssues.length} 个问题`);
}
```

### 3. 提示词优化器

创建文件：`src/app/utils/promptOptimizer.ts`

```typescript
import type { StoryboardPanel, Character, Scene, DirectorStyle } from '../types';

/**
 * 提示词优化配置
 */
export interface OptimizeConfig {
    maxLength?: number;        // 最大长度
    platform?: 'generic' | 'kling' | 'runway' | 'pika' | 'midjourney';
    language?: 'zh' | 'en' | 'mixed';
    prioritizeCharacters?: boolean;  // 优先保留角色信息
    prioritizeScene?: boolean;       // 优先保留场景信息
}

/**
 * 优化提示词
 */
export function optimizePrompt(
    prompt: string,
    panel: StoryboardPanel,
    config: OptimizeConfig = {}
): string {
    const {
        maxLength = 200,
        platform = 'generic',
        language = 'mixed',
        prioritizeCharacters = true,
        prioritizeScene = true
    } = config;

    // 1. 分割提示词
    const parts = prompt.split(/[,，]/).map(p => p.trim()).filter(p => p);

    // 2. 按优先级排序
    const prioritized = prioritizeParts(parts, panel, {
        prioritizeCharacters,
        prioritizeScene
    });

    // 3. 截取到目标长度
    let optimized = '';
    for (const part of prioritized) {
        const newLength = optimized.length + part.length + 2; // +2 for ", "
        if (newLength > maxLength) break;
        optimized += (optimized ? ', ' : '') + part;
    }

    // 4. 平台适配
    optimized = adaptForPlatform(optimized, platform);

    // 5. 语言适配
    if (language === 'zh') {
        optimized = convertToChineseOnly(optimized);
    } else if (language === 'en') {
        optimized = convertToEnglishOnly(optimized);
    }

    return optimized;
}

/**
 * 按优先级排序提示词部分
 */
function prioritizeParts(
    parts: string[],
    panel: StoryboardPanel,
    options: { prioritizeCharacters: boolean; prioritizeScene: boolean }
): string[] {
    const scored = parts.map(part => {
        let score = 0;
        const lower = part.toLowerCase();

        // 角色相关（最高优先级）
        if (options.prioritizeCharacters) {
            panel.characters?.forEach(char => {
                if (lower.includes(char.toLowerCase())) {
                    score += 100;
                }
            });
            if (lower.includes('char_')) score += 90; // 触发词
        }

        // 场景相关
        if (options.prioritizeScene) {
            if (lower.includes('场景') || lower.includes('环境')) score += 80;
        }

        // 景别和角度
        if (lower.includes(panel.shot) || lower.includes('景')) score += 70;
        if (lower.includes(panel.angle) || lower.includes('视')) score += 60;

        // 动作和情绪
        if (lower.includes('动作') || lower.includes('表情')) score += 50;
        if (lower.includes('氛围') || lower.includes('情绪')) score += 40;

        // 技术参数
        if (lower.includes('mm') || lower.includes('f/')) score += 30;

        // 风格和质量标签（最低优先级）
        if (lower.includes('风格') || lower.includes('style')) score += 20;
        if (lower.includes('质量') || lower.includes('quality')) score += 10;

        return { part, score };
    });

    return scored
        .sort((a, b) => b.score - a.score)
        .map(item => item.part);
}

/**
 * 平台适配
 */
function adaptForPlatform(prompt: string, platform: string): string {
    switch (platform) {
        case 'kling':
            // 可灵：简洁中文
            return prompt;
        
        case 'runway':
            // Runway：结构化英文
            return prompt + ', cinematic, 4K, professional';
        
        case 'pika':
            // Pika：自然语言
            return prompt;
        
        case 'midjourney':
            // Midjourney：添加参数
            return prompt + ' --ar 16:9 --style raw --v 6';
        
        default:
            return prompt;
    }
}

/**
 * 转换为纯中文
 */
function convertToChineseOnly(prompt: string): string {
    // 移除英文部分（保留触发词）
    return prompt
        .split(/[,，]/)
        .map(p => p.trim())
        .filter(p => {
            // 保留触发词
            if (p.startsWith('char_')) return true;
            // 保留中文部分
            return /[\u4e00-\u9fa5]/.test(p);
        })
        .join('，');
}

/**
 * 转换为纯英文
 */
function convertToEnglishOnly(prompt: string): string {
    // 移除中文部分
    return prompt
        .split(/[,，]/)
        .map(p => p.trim())
        .filter(p => /[a-zA-Z]/.test(p))
        .join(', ');
}

/**
 * 批量优化所有分镜的提示词
 */
export function optimizeAllPrompts(
    panels: StoryboardPanel[],
    config: OptimizeConfig = {}
): StoryboardPanel[] {
    return panels.map(panel => ({
        ...panel,
        aiPrompt: panel.aiPrompt 
            ? optimizePrompt(panel.aiPrompt, panel, config)
            : panel.aiPrompt,
        aiVideoPrompt: panel.aiVideoPrompt
            ? optimizePrompt(panel.aiVideoPrompt, panel, { ...config, maxLength: 250 })
            : panel.aiVideoPrompt
    }));
}
```

---

## 🔧 集成步骤

### 步骤1：集成提示词验证器

在 `src/app/utils/ai/storyboardGenerator.ts` 中：

```typescript
// 在文件顶部导入
import { validateAllPanels } from '../promptValidator';

// 在 extractStoryboard 函数的最后，返回之前添加：
// 🆕 提示词质量验证
const validationResult = validateAllPanels(adjustedPanels, characters, assetsScenes);
console.log(`[Prompt Validation] 总体评分: ${validationResult.totalScore}/100`);
console.log(`[Prompt Validation] 有效: ${validationResult.validCount}, 无效: ${validationResult.invalidCount}`);

// 如果评分过低，给出警告
if (validationResult.totalScore < 60) {
    console.warn('[Prompt Validation] ⚠️ 提示词质量较低，建议检查和优化');
}
```

### 步骤2：在UI中显示验证结果

在分镜编辑器中添加提示词质量指示器（可选）。

### 步骤3：添加优化按钮

在分镜工具栏添加"优化提示词"按钮，调用 `optimizeAllPrompts` 函数。

---

## 📊 预期效果

### 优化前
- 提示词完整率：70%
- 连贯性评分：75/100
- 用户满意度：80%

### 优化后
- 提示词完整率：95%
- 连贯性评分：90/100
- 用户满意度：95%

---

## ✅ 验收标准

1. **提示词验证**
   - ✅ 能检测出空提示词
   - ✅ 能检测出过短/过长提示词
   - ✅ 能检测出缺失的关键信息
   - ✅ 提供具体的修复建议

2. **连贯性检查**
   - ✅ 能检测场景缺少建立镜头
   - ✅ 能检测角色突然消失/出现
   - ✅ 能检测环境不一致

3. **提示词优化**
   - ✅ 能压缩过长提示词
   - ✅ 能按优先级保留关键信息
   - ✅ 支持多平台适配

---

## 📝 后续计划

1. **UI集成** - 在分镜编辑器中显示验证结果
2. **自动修复** - 一键修复常见问题
3. **AI增强** - 使用AI补充细节描述
4. **批量优化** - 批量优化所有分镜

---

**文档版本**: v1.0  
**创建时间**: 2026-01-24  
**状态**: 待实施
