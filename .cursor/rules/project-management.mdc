# 项目管理模块 Skills

## 模块概述
项目管理模块负责AI漫剧项目的创建、管理、删除等核心功能。

## 核心文件
- `src/app/pages/Bookshelf.tsx` - 书架页面（项目列表）
- `src/app/pages/ProjectDetail.tsx` - 项目详情页
- `src/app/pages/ProjectDashboard.tsx` - 项目数据统计
- `src/app/utils/storage.ts` - 数据存储层
- `src/app/db/db.ts` - IndexedDB 数据库定义
- `src/app/types/project.ts` - 项目类型定义

## 功能实现

### 1. 项目创建
```typescript
// 位置: src/app/pages/Bookshelf.tsx
const handleCreateProject = async () => {
  const project: Project = {
    id: generateId(),
    title: newProject.title,
    description: newProject.description,
    cover: '',
    createdAt: new Date().toISOString(),
    updatedAt: new Date().toISOString(),
  };
  await projectStorage.save(project);
};
```

### 2. 项目删除（级联删除）
```typescript
// 位置: src/app/utils/storage.ts
// 删除项目时会级联删除所有关联数据：
// - 章节 (chapters)
// - 剧本 (scripts)
// - 分镜 (storyboards)
// - 资源库 (assets)
// - 版本历史 (versions)
// - 角色关系 (relations)
```

### 3. 章节管理
```typescript
// 位置: src/app/pages/ProjectDetail.tsx
// 功能：创建、查看、管理项目下的章节
// 每个章节包含：原文编辑、剧本改写、分镜制作三个子功能
```

## 数据模型

### Project 类型
```typescript
interface Project {
  id: string;
  title: string;
  cover: string;
  description: string;
  directorStyle?: DirectorStyle;  // 导演风格设定
  createdAt: string;
  updatedAt: string;
  stats?: ProjectStats;  // 项目统计
}
```

### Chapter 类型
```typescript
interface Chapter {
  id: string;
  projectId: string;
  title: string;
  orderIndex: number;
  originalText: string;  // 原文内容
  createdAt: string;
}
```

## 存储层 API

### projectStorage
- `getAll()` - 获取所有项目
- `getById(id)` - 根据ID获取项目
- `save(project)` - 保存/更新项目
- `delete(id)` - 删除项目（级联删除）

### chapterStorage
- `getByProjectId(projectId)` - 获取项目下所有章节
- `getById(id)` - 根据ID获取章节
- `save(chapter)` - 保存/更新章节
- `delete(id)` - 删除章节

## 开发规范

1. **ID生成**: 使用 `generateId()` 函数生成唯一ID
2. **时间戳**: 使用 ISO 8601 格式 (`new Date().toISOString()`)
3. **数据持久化**: 使用 IndexedDB (Dexie.js)
4. **响应式查询**: 使用 `useLiveQuery` 实现数据实时更新
5. **Toast提示**: 使用 `sonner` 库的 `toast` 函数

## 注意事项

- 删除项目前必须显示确认对话框
- 项目列表支持搜索和排序功能
- 项目卡片支持悬停预览效果
